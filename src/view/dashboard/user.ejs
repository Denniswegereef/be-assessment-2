<%- include('../component/head.ejs'); %>
<%- include('../component/header.ejs'); %>

<main class="container user-profile">
    <section>
        <h1>User</h1>

        <% if (data.info.image){ %>
        <img src="../images/users/<%= data.info.image %>" alt="" class="profile-image">
        <% } else { %>
        <img src="../images/users/profile-placeholder.jpg" alt="" class="profile-image">
        <% } %>

        <h2><%= data.user %>, <%= data.info.age %></h2>
        <h3 class="place"><img src="../images/icons/place.svg" alt=""><%= data.info.place %></h3>

        <ul>
            <li><span>Geslacht:</span> <%= data.info.gender === 'male' ? 'man' : 'vrouw' %> </li>
            <% if (data.info.study){ %>
            <li><span>Studie:</span> <%= data.info.study %></li>
            <% } %>
            <% if (data.info.work){ %>
            <li><span>Studie:</span> <%= data.info.work %></li>
            <% } %>
            <li>Opzoek naar <%= data.preference.sex === 'male' ? 'mannen' : 'vrouwen' %>
                tussen de <span><%= data.preference.ageMin %></span> en
                <span><%= data.preference.ageMax %></span> jaar
            </li>
        </ul>

        <% if (data.info.additional){ %>
        <p><span>Dit wil ik nog kwijt over mijzelf.. </span><br>
            <%= data.info.additional %></p>
        <% } %>

        <h3>Favoriete films</h3>
        <ul>
            <% data.movies.forEach(function (movie) { %>
            <li><%= movie.name %></li>
            <% }) %>
        </ul>

        <p class="created">Aangemaakt op <%= data.info.accountCreated %></p>
    </section>

</main>
<%- include('../component/footer.ejs'); %>

